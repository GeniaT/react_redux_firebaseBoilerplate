const initialState = [];

export default function reducerName2(state = initialState, action) {
  switch (action.type) {
    case 'ACTIONADUCK2':
      return state;
      break;
    case 'ACTIONBDUCK2':
      return state;
      break;
    case 'ACTIONCDUCK2':
      return state;
      break;
    default:
      return state
  }
}
