const initialState = [];

export default function reducerName3(state = initialState, action) {
  switch (action.type) {
    case 'ACTIONADUCK3':
      return state;
      break;
    case 'ACTIONBDUCK3':
      return state;
      break;
    case 'ACTIONCDUCK3':
      return state;
      break;
    default:
      return state
  }
}
